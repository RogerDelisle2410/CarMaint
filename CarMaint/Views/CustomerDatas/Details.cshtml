@model CarMaint.Models.CustomerData

@{
    string lang = "en";
    if (Request.Cookies["lang"] != null)
    {
        lang = Request.Cookies["lang"].Value;
    }

    string jsonPath = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "Lang", lang + ".json");
    string jsonText = System.IO.File.ReadAllText(jsonPath);
    var Lang = Newtonsoft.Json.Linq.JObject.Parse(jsonText);

    ViewBag.Title = Lang["details_title"].ToString();
}

<h2>@Lang["details_title"]</h2>

<div>
    <h4>@Lang["details_customer_info"]</h4>
    <hr />

    <dl class="dl-horizontal">

        <dt>@Lang["col_name"]</dt>
        <dd>@Html.DisplayFor(model => model.Name)</dd>

        <dt>@Lang["col_address"]</dt>
        <dd>@Html.DisplayFor(model => model.Address)</dd>

        <dt>@Lang["col_city"]</dt>
        <dd>@Html.DisplayFor(model => model.City)</dd>

        <dt>@Lang["col_postal"]</dt>
        <dd>@Html.DisplayFor(model => model.PostalCode)</dd>

        <dt>@Lang["col_phone"]</dt>
        <dd>@Html.DisplayFor(model => model.Phone)</dd>

    </dl>
</div>

<p>
    @Html.ActionLink(Lang["details_edit"].ToString(), "Edit", new { id = Model.CustomerId })
    |
    @Html.ActionLink(Lang["details_back_to_list"].ToString(), "Index")
</p>
